# Automating A Genomic Interval Analysis by Snakemake

To run the pepline, `sbatch WRAPPER_SLURM` at root directory.

The presentation is in the `presentation` folder. 

The report is in the `report` folder. 
